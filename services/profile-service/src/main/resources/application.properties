spring.application.name=profile-service
server.port=8080

# ALB target-group health checks hit /actuator/health
management.endpoints.web.exposure.include=health,info
management.endpoint.health.probes.enabled=true
